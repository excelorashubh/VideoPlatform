import { AppShell, PageHeading } from "../_components/AppShell";

export default function StudioPage() {
  return <AppShell active="/studio" eyebrow="Creator Studio" title="Your channel at a glance"><div className="metric-grid"><article className="metric"><span>Views this month</span><strong>48.2k</strong><small>↑ 18% from last month</small></article><article className="metric"><span>Watch time</span><strong>1,284h</strong><small>↑ 9% from last month</small></article><article className="metric"><span>Subscribers</span><strong>8,942</strong><small>+312 this month</small></article></div><PageHeading eyebrow="Latest activity" title="Channel pulse" action="Open analytics" /><div className="studio-grid"><section className="panel"><div className="panel-heading"><h3>Recent content</h3><span>Last 28 days</span></div>{["The quiet architecture of good systems", "Field notes from a moving planet", "How sound becomes a place"].map((title, index) => <div className="activity-row" key={title}><div className={`mini-thumb tone-${index + 1}`} /><div><strong>{title}</strong><span>{index + 1 === 1 ? "Published" : "Processing"} · {12 + index * 7}.4k views</span></div><b>{index === 0 ? "92%" : "--"}</b></div>)}</section><section className="panel accent-panel"><p className="kicker">Next useful move</p><h3>Keep your audience close.</h3><p>Reply to recent comments and keep the conversation moving around your newest upload.</p><a className="text-link" href="/studio/comments">Review comments</a></section></div></AppShell>;
}

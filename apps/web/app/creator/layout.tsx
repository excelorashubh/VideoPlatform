import { CreatorLayoutFrame } from "./_components/CreatorLayoutFrame";

export default function CreatorLayout({ children }: { children: React.ReactNode }) {
  return <CreatorLayoutFrame>{children}</CreatorLayoutFrame>;
}
